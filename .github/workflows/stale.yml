name: Stale

on:
  schedule:
    - cron: "0 2 * * *"

permissions:
  issues: write
  pull-requests: write

jobs:
  stale:
    uses: dallay/common-actions/.github/workflows/stale.yml@v2.2.3
    secrets: inherit
