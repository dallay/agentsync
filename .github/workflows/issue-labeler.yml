name: Issue Labeler

on:
  issues:
    types: [opened, edited]

permissions:
  issues: write

jobs:
  label:
    uses: dallay/common-actions/.github/workflows/issue-labeler.yml@v2.0.0
    secrets: inherit
